# =====================================================================
# NeuroSense AI — Backend Deployment Template (Render)
# =====================================================================
# Copy/Upload this exact text to Render as a Secret File named `.env`:

PORT=10000
SECRET_KEY=neurosense-secure-jwt-secret-key-2026-prod
FLASK_ENV=production
LOG_LEVEL=INFO
PYTHON_VERSION=3.11.9

# Allowed CORS Origins (Vercel Frontend + Localhost):
CORS_ORIGINS=https://neurosense-ai.vercel.app,http://localhost:5173,http://localhost:3000

# Multi-Provider AI Recommender Keys (Optional):
GEMINI_API_KEY=
GROQ_API_KEY=
OPENAI_API_KEY=
